# Secret Santa Telegram Bot

Telegram бот для игры в Secret Santa с Django админ-панелью.

## Функционал

- 🎅 Администратор добавляет участников через Django Admin
- 📱 Пользователи регистрируются через Telegram Bot по номеру телефона
- 🎁 Автоматическое распределение Secret Santa
- 🔐 Каждый видит только своего получателя подарка

## Установка

1. Создайте виртуальное окружение:
```powershell
python -m venv venv
.\venv\Scripts\Activate.ps1
```

2. Установите зависимости:
```powershell
pip install -r requirements.txt
```

3. Скопируйте `.env.example` в `.env` и заполните:
```powershell
copy .env.example .env
```

4. Отредактируйте `.env`:
   - `TELEGRAM_BOT_TOKEN` - токен от @BotFather
   - `SECRET_KEY` - секретный ключ Django (можно сгенерировать)
   - `DEBUG` - True для разработки, False для продакшена

## Запуск

1. Примените миграции:
```powershell
python manage.py makemigrations
python manage.py migrate
```

2. Создайте суперпользователя:
```powershell
python manage.py createsuperuser
```

3. Запустите Django сервер:
```powershell
python manage.py runserver
```

4. В другом терминале запустите бота:
```powershell
python bot.py
```

## Использование

### Настройка игры (Администратор)

1. Откройте админ-панель: http://127.0.0.1:8000/admin/
2. Добавьте участников (Имя + Номер телефона)
3. Создайте игровую сессию
4. Выберите сессию и используйте действие "Сгенерировать распределение Secret Santa"

### Участие в игре (Пользователи)

1. Откройте бота в Telegram
2. Нажмите `/start`
3. Поделитесь номером телефона
4. Бот покажет вам, кому вы дарите подарок

## Структура проекта

```
secret santa bot/
├── secret_santa/          # Django проект
│   ├── settings.py
│   ├── urls.py
│   └── ...
├── santa_game/            # Django приложение
│   ├── models.py         # Модели БД
│   ├── admin.py          # Админ-панель
│   └── utils.py          # Логика игры
├── bot.py                # Telegram бот
├── manage.py             # Django CLI
├── requirements.txt      # Зависимости
└── README.md            # Документация
```

## Модели

- **Participant** - Участник игры (имя, номер телефона, Telegram ID)
- **SecretSantaMatch** - Распределение (кто кому дарит)
- **GameSession** - Игровая сессия

## Команды бота

- `/start` - Начать игру и узнать своего получателя
- `/help` - Справка

## Безопасность

⚠️ Для продакшена:
- Измените `SECRET_KEY` на уникальный
- Установите `DEBUG=False`
- Настройте `ALLOWED_HOSTS`
- Используйте PostgreSQL вместо SQLite
- Настройте HTTPS

# 🚀 Инструкция по деплою на PythonAnywhere

## Шаг 1: Создание аккаунта
1. Зарегистрируйтесь на https://www.pythonanywhere.com
2. Выберите бесплатный план (Free tier)

## Шаг 2: Загрузка проекта

### Вариант А: Через GitHub (Рекомендуется)
1. Загрузите проект на GitHub
2. В PythonAnywhere откройте консоль Bash
3. Клонируйте репозиторий:
```bash
git clone https://github.com/YOUR_USERNAME/YOUR_REPO.git
cd YOUR_REPO
```

### Вариант Б: Загрузка файлов
1. Перейдите в раздел "Files"
2. Создайте папку `secret-santa-bot`
3. Загрузите все файлы проекта

## Шаг 3: Настройка виртуального окружения

Откройте Bash консоль и выполните:

```bash
cd ~/secret-santa-bot
python3.11 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

## Шаг 4: Создание .env файла

```bash
nano .env
```

Добавьте:
```
SECRET_KEY=ваш-секретный-ключ-сюда
DEBUG=False
TELEGRAM_BOT_TOKEN=ваш-токен-бота
```

Сохраните: Ctrl+O, Enter, Ctrl+X

## Шаг 5: Применение миграций

```bash
python manage.py migrate
python manage.py collectstatic --noinput
python manage.py createsuperuser
```

## Шаг 6: Настройка Web App

1. Перейдите в раздел "Web"
2. Нажмите "Add a new web app"
3. Выберите "Manual configuration"
4. Выберите Python 3.11

### Настройка WSGI файла:

1. В разделе "Code" найдите "WSGI configuration file"
2. Нажмите на ссылку (например: `/var/www/username_pythonanywhere_com_wsgi.py`)
3. Удалите весь код
4. Скопируйте содержимое из `pythonanywhere_wsgi.py`
5. Замените `YOUR_USERNAME` на ваш username
6. Сохраните

### Настройка виртуального окружения:

В разделе "Virtualenv":
```
/home/YOUR_USERNAME/secret-santa-bot/venv
```

### Настройка статических файлов:

В разделе "Static files":
- URL: `/static/`
- Directory: `/home/YOUR_USERNAME/secret-santa-bot/staticfiles/`

## Шаг 7: Запуск бота

Создайте файл для запуска бота в фоне:

```bash
cd ~/secret-santa-bot
nano start_bot.sh
```

Добавьте:
```bash
#!/bin/bash
cd /home/YOUR_USERNAME/secret-santa-bot
source venv/bin/activate
python bot.py
```

Сохраните и сделайте исполняемым:
```bash
chmod +x start_bot.sh
```

### Настройка автозапуска бота:

В PythonAnywhere перейдите в "Tasks" и добавьте:

**Schedule**: Daily (или когда нужно)
**Command**: `/home/YOUR_USERNAME/secret-santa-bot/start_bot.sh`

Или запустите вручную в консоли:
```bash
cd ~/secret-santa-bot
source venv/bin/activate
nohup python bot.py > bot.log 2>&1 &
```

## Шаг 8: Перезагрузка веб-приложения

1. В разделе "Web" нажмите зелёную кнопку "Reload"
2. Откройте ваш сайт: `https://YOUR_USERNAME.pythonanywhere.com/admin/`

## 🎉 Готово!

### Доступы:
- **Admin панель**: https://YOUR_USERNAME.pythonanywhere.com/admin/
- **Telegram бот**: должен работать 24/7

## 📝 Важные заметки:

1. **Бесплатный план ограничения:**
   - Одно веб-приложение
   - Бот должен быть запущен отдельным процессом
   - Нужно перезапускать бота каждые 24 часа (автоматизируйте через Tasks)

2. **Обновление кода:**
```bash
cd ~/secret-santa-bot
git pull  # если используете git
source venv/bin/activate
python manage.py migrate
python manage.py collectstatic --noinput
# Перезапустите веб-приложение через Web раздел
# Перезапустите бота
```

3. **Просмотр логов бота:**
```bash
tail -f ~/secret-santa-bot/bot.log
```

4. **Остановка бота:**
```bash
pkill -f bot.py
```

## 🆘 Проблемы?

1. Проверьте логи ошибок в разделе "Web" -> "Error log"
2. Проверьте логи бота: `cat ~/secret-santa-bot/bot.log`
3. Убедитесь что все пути правильные (замените YOUR_USERNAME)
4. Проверьте что .env файл существует и заполнен

## 🔄 Альтернатива для бота

Если бот не работает стабильно на PythonAnywhere, используйте отдельный сервис только для бота:
- **Render.com** - бесплатно
- **Railway.app** - $5 кредитов/месяц
- **Fly.io** - бесплатно

А Django админку держите на PythonAnywhere.
